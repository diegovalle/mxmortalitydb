library(testthat)
library(mxmortalitydb)
library(dplyr)

test_check("testthat")

# Encoding(aggressor.relation.code$relationship) <- "UTF-8"
# save(aggressor.relation.code, file = "data/aggressor.relation.code.RData",
#      compress = "xz")
# 
# Encoding(big.municipios$name) <- "UTF-8"
# save(big.municipios, file = "data/big.municipios.RData",
#      compress = "xz")
# 
# Encoding(geo.codes$name) <- "UTF-8"
# save(geo.codes, file = "data/geo.codes.RData",
#      compress = "xz")
# 
# Encoding(icd.103$icd_103_name) <- "UTF-8"
# save(icd.103, file = "data/icd.103.RData",
#      compress = "xz")
# 
# Encoding(metro.areas$metro_area) <- "UTF-8"
# save(metro.areas, file = "data/metro.areas.RData",
#      compress = "xz")
# 
# Encoding(mex.list$DESCRIP) <- "UTF-8"
# save(mex.list, file = "data/mex.list.RData",
#      compress = "xz")
# 
# Encoding(mex.list.group$mex_list_group_name) <- "UTF-8"
# save(mex.list.group, file = "data/mex.list.group.RData",
#      compress = "xz")
